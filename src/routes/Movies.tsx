import MoviesList from "../components/MoviesList";

export default function Movies() {
  // Idea use carousel
  // display one movie at a time
  // half carousel is image other half is info
  // caoursel is infinite
  // item is a card
  return <MoviesList />;
}
